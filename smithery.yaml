# smithery.yaml
# Docs: smithery runtime must be "python" | "typescript" | "container"
name: homeharvest-mcp
version: 0.1.0

runtime: "python"

# Python runtime options
python:
  packageManager: "uv"     # tells Smithery to use uv behind the scenes

# Your MCP server entrypoint (stdio server)
entry: "src/homeharvest_mcp/server.py"
